spring:
    application:
        name: eureka-service

server:
    port: 8701

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8701/eureka/
    healthcheck:
      enabled: true
    register-with-eureka: true
    fetch-registry: true


